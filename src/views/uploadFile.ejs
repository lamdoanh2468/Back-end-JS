<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home Page</title>
    <link rel="icon" type="image/png" href="images/nodejs.png" />
    <link rel="stylesheet" href="https://www.w3schools.com/w3css/5/w3.css" />
    <link rel="stylesheet" href="/css/home.css" />
    <script>
        function confirmDelete(userID) {
            const isConfirmed = confirm("Are you sure for removing this user?");
            if (isConfirmed) {
                window.location.href = `remove_user/${userID}`;
            }
        }
    </script>
</head>

<body>
    <div class="topnav">
        <a href="/">Home</a>
        <a href="/create">Create User</a>
        <a class="active" href="/upload">Upload File</a>
    </div>

    <div class="upload-container">
        <form method="post" action="/upload-profile-pic" enctype="multipart/form-dataf">
            <div>
                <label for="">Avatar:</label>
                <input type="file" name="profile_pic" multiple>
            </div>
              <div>
                <label for="">Avatar:</label>
                <input type="file" name="profile_pic" >
            </div>
            <div>
                <button class="save-btn" type="submit">Save files</button>
            </div>
        </form>
    </div>
</body>

</html>